---
version: "0.4.0"
date: "2026-08-14"
title: "Six new AI features — dubbing, background removal, B-roll, captions, speakers, multicam"
description: "The biggest release yet: AI dubbing with fully-local translation, video background removal, auto B-roll from your own footage, multilingual captions, edit-by-speaker, and automatic multicam sync. Plus visual footage search, thumbnail A/B testing, and a hardened, deployable backend."
changes:
  - type: new
    text: "AI dubbing with a fully-local engine — a new NLLB-200 translate-service (port 8427) batch-translates transcripts on-device, with Sarvam cloud fallback for Indian language pairs and LLM fallback as a last resort. A full dubbing pipeline (Whisper → translate → XTTS → ffmpeg) is also available at /api/dub/*."
  - type: new
    text: "Video background removal — per-frame matting via rembg on the image-service, with job status polling. The result is inserted non-destructively as an alpha WebM layer above the source clip."
  - type: new
    text: "Auto B-roll from your own footage — each transcript segment is matched against the existing CLIP embedding index (IndexedDB) and the best clip is auto-inserted via /api/broll/*. Reuses the visual-search index, no parallel store."
  - type: new
    text: "Multilingual captions — NLLB-first translation with multi-language quick-add chips and per-track SRT/VTT export."
  - type: new
    text: "Edit by speaker — scope remove, tighten-gaps, and isolate operations to a single speaker using diarization. Cut/compact helpers are shared with Smart Cut via lib/timeline-edits.ts."
  - type: new
    text: "Automatic multicam sync — client-side audio cross-correlation aligns camera angles without timecode. Pick a reference angle, preview offsets with confidence scores, and apply in one undoable transaction. Pure Web Audio, no backend round-trip."
  - type: new
    text: "Local CLIP-powered visual search — search your footage by meaning, powered by on-device CLIP embeddings."
  - type: new
    text: "Thumbnail A/B testing, AI copilot, YouTube viral score, TurboQuant quantized models, and video templates."
  - type: new
    text: "New model support: Sarvam AI (Indian languages), Smallest AI, Kimi K2, and Google Gemma. GPU-accelerated docker-compose profile."
  - type: improved
    text: "Backend job queue parameterized per-domain Redis prefixes so dubbing and background-removal jobs never collide with YouTube analytics jobs. Translate-service added to docker-compose and the GPU override."
  - type: improved
    text: "Upload endpoints hardened — payload size limits, extension allowlists, filename sanitization, generic error messages, and narrowed CORS."
  - type: improved
    text: "VPS deployment stabilized — fixed play/pause bootloop from shuttle speed=0, removed browser localhost/telemetry calls, hardened UUID generation, and repaired transitions, sharpen effect, and audio effects wiring."
  - type: fixed
    text: "Version-control initialization race — concurrent component mounts can no longer both create the main branch; in-flight initialization is now shared per project."
  - type: fixed
    text: "Missing pydantic Field import in engagement routes."
  - type: fixed
    text: "Web build repaired — a scope error in the captions translation helper, an unclosed JSX tag in the multicam panel, an incomplete ternary in the sync toast, and a mismatched structural editor interface all blocked compilation."
  - type: fixed
    text: "Install, lint, and CI tooling — bun.lock regenerated to match package.json so frozen installs work, the missing Biome dependency restored so lint scripts run, build artifacts untracked, and the CI cache key pointed at the real lockfile with the actual test suite wired up."
---
